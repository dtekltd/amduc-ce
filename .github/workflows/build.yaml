name: Build PCMT Server

on:
  workflow_dispatch:
    inputs:
      version:
        required: true
        type: string

permissions:
  contents: write
  packages: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
        with:
          repository: "dtekltd/pcmt"
          token: ${{ secrets.WORKFLOW_PAT }}
          ref: main
          fetch-depth: 0
          fetch-tags: true

      - name: Checkout Tag (if version != 0)
        if: ${{ inputs.version != '0' }}
        run: |
          git checkout ${{ inputs.version }}

      - name: Ensure Main (if version == 0)
        if: ${{ inputs.version == '0' }}
        run: |
          echo "Version is 0, staying on main branch"

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "24.0.1"
          cache: "npm"
          cache-dependency-path: "acp/package-lock.json"

      - name: Build ACP App
        working-directory: ./acp
        run: |
          npm install
          npm run build

      - name: Setup Go
        uses: actions/setup-go@v4
        with:
          go-version: "1.24"
          cache: true

      - name: Build PCMT Server
        working-directory: .
        run: |
          set -e
          CGO_ENABLED=0 GOOS=linux GOARCH=amd64 go build -o ./pcmt .

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v4
        with:
          context: .
          push: true
          tags: |
            ghcr.io/${{ github.repository_owner }}/pcmt-ce:latest
